{
  "name": "koraalenviajeugd",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "deploy": "node -e \"const cp=require('child_process');const v=require('./package.json').version;let c='';try{c=cp.execSync('git rev-parse --short HEAD').toString().trim()}catch(e){}require('fs').writeFileSync('public/version.json',JSON.stringify({version:v,commit:c,date:new Date().toISOString()}))\" && npx --yes wrangler deploy",
    "dev": "wrangler dev"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250612.0",
    "typescript": "^5.5.0",
    "wrangler": "^4.0.0"
  }
}
